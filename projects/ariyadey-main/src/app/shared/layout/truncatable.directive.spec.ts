import { TruncatableDirective } from "@main/shared/layout/truncatable.directive";

describe("TruncatableDirective", () => {
  it("should create an instance", () => {
    const directive = new TruncatableDirective();
    expect(directive).toBeTruthy();
  });
});
